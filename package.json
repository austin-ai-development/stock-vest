{
    "name": "stock-vest",
    "version": "1.0.0",
    "description": "StockVest Mono-repo Root",
    "scripts": {
        "start": "cd server && npm start",
        "install-client": "cd client && npm install",
        "install-server": "cd server && npm install",
        "heroku-postbuild": "npm run install-client && cd client && npm run build && cd .. && npm run install-server && cd server && npm run build"
    },
    "engines": {
        "node": "18.x"
    }
}